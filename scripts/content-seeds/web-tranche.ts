export type WebTeachingSeed = readonly [
  model: string,
  boundary: string,
  scenario: string,
  misconception: string,
  correction: string,
  checkpoint: string,
];

export const WEB_TRANCHE_SEEDS = {
  "html.document.syntax": ["HTML tokenization and tree construction interpret elements, attributes, and text into a document structure.", "Parser error recovery exists but is not a substitute for conforming markup and can produce unexpected trees.", "The fragment <p>Hello <em>friend</em></p> creates nested paragraph, emphasis, and text nodes.", "HTML is executed line by line exactly like JavaScript source.", "HTML is parsed into a document tree; author conforming markup and inspect the resulting DOM rather than relying on recovery.", "parsed into a document tree"],
  "html.document.skeleton": ["A standards-mode document declares the HTML doctype and provides html, head, and body structure with language and metadata.", "Browsers can infer omitted elements, but explicit structure supports tooling, accessibility, and predictable maintenance.", "A page begins with <!doctype html>, sets lang on html, gives a title in head, and puts visible content in body.", "The doctype is an optional comment that only documents the HTML version.", "Use <!doctype html> to request standards mode and provide explicit html, head, and body structure.", "standards mode"],
  "html.document.dom": ["The DOM is the browser's object-tree representation of the parsed document, not the original source text itself.", "Parser repair, script mutation, templates, and developer tools can make the live DOM differ from view-source text.", "A missing table wrapper is inserted by parsing rules, so the inspected DOM contains nodes absent from typed source.", "The DOM and the saved HTML file are always byte-for-byte identical.", "Treat the DOM as the live parsed tree; inspect it when reasoning about structure because it can differ from source text.", "live parsed tree"],
  "html.document.comments-entities": ["HTML comments annotate source without becoming rendered text, while character references represent characters that would otherwise be syntax or hard to type.", "Comments are visible to anyone receiving source and must never contain secrets; unnecessary entities can reduce readability.", "The source writes &lt;code&gt; to display literal angle brackets instead of creating a code element.", "HTML comments are private and safe for API keys because browsers do not display them.", "Comments remain in delivered source and are not private; use character references only when needed to represent data safely.", "not private"],
  "html.semantics.headings": ["Headings label document sections and form a meaningful hierarchy that supports scanning and assistive navigation.", "Heading rank should reflect structure rather than visual size, and skipping ranks can obscure relationships even when browsers render them.", "A page uses one descriptive h1 for its main subject and h2 elements for peer sections such as syllabus and schedule.", "Heading elements should be chosen by the font size the design needs.", "Choose heading rank from section hierarchy, then control visual size with CSS rather than selecting headings by appearance.", "section hierarchy"],
  "html.semantics.text": ["Paragraph, emphasis, strong importance, quotation, code, time, and related text elements communicate meaning beyond visual styling.", "Element semantics do not prescribe one appearance, and em or strong should not be used merely to obtain italics or bold.", "A warning uses strong for importance while a variable name uses code, allowing meaning to survive without CSS.", "A span with bold CSS is semantically identical to strong in every context.", "Use the text element whose meaning matches the content; styling alone does not add strong importance or emphasis semantics.", "meaning matches the content"],
  "html.semantics.lists": ["Ordered lists communicate sequence, unordered lists communicate a set without ranking, and description lists associate terms with descriptions.", "Visual bullets alone do not create list semantics, and navigation menus are usually lists of links rather than arbitrary div rows.", "Recipe steps use ol because changing order changes the result, while ingredient names use ul.", "All groups of repeated text should use ul because list order never affects meaning.", "Choose ol when sequence is meaningful, ul when it is not, and preserve real list elements instead of visual imitation.", "sequence is meaningful"],
  "html.semantics.landmarks": ["Native header, nav, main, aside, and footer elements expose page regions and support landmark navigation when used for their intended roles.", "Multiple landmarks of one type may need accessible labels, and header or footer landmark behavior depends on nesting context.", "A page has one main content region and labels two nav regions as Primary and Course topics.", "Every div should be replaced with a landmark element to maximize accessibility.", "Use landmarks only for genuine page regions, keep one primary main region, and label repeated landmark types distinctly.", "genuine page regions"],
  "html.navigation.urls": ["A URL combines scheme, authority, path, query, and fragment, and relative references resolve against the document base URL.", "Encoding, trailing slashes, base elements, origin rules, and deployment subpaths can change resolution.", "From /courses/html/index.html, the relative link ../css/ resolves to /courses/css/ under the current base.", "Relative URLs always resolve from the website root.", "Resolve relative URLs against the document base URL, not automatically against the origin root.", "document base URL"],
  "html.navigation.links": ["An anchor with href creates navigation, and its accessible name should describe the destination or action in context.", "Placeholder anchors, click-only spans, repeated vague names, and invalid nested interactive controls undermine keyboard and assistive use.", "A link named Read the C arrays lesson communicates its destination without surrounding visual context.", "Any element with a click handler is automatically equivalent to an anchor.", "Use an anchor with href for navigation and give it a destination-specific accessible name.", "anchor with href"],
  "html.navigation.external": ["External links should disclose meaningful destination behavior and apply deliberate new-context and referrer policies where required.", "Opening a new tab is not always helpful, browser behavior evolves, and rel tokens do not replace URL trust or user communication.", "A link identifies that documentation opens on an external site and avoids forcing a new context unless the product requires it.", "Every external link must open a new tab because that is always safer and more accessible.", "Do not force a new browsing context by default; when one is required, disclose it and apply the appropriate rel policy.", "disclose it"],
  "html.navigation.skip": ["A skip link is the first practical keyboard shortcut to the primary content target and becomes visible when focused.", "Its target must exist and receive usable focus or scroll behavior, and it does not replace sensible landmarks and heading order.", "Pressing Tab first reveals Skip to main content and activating it moves navigation past a long site menu.", "Landmarks make skip links unnecessary for every keyboard user.", "Provide a visible-on-focus skip link to the main content target alongside correct landmarks and heading structure.", "skip link"],
  "html.media.images": ["Image alternative text communicates the image's purpose in context, while decorative images use an empty alt so they are ignored.", "Alt text depends on surrounding content; filenames, image of prefixes, and redundant captions are usually poor substitutes.", "A chart image receives concise alt identifying its conclusion and a nearby detailed data table.", "Every image needs a long visual description in its alt attribute.", "Write purpose-based alt text for informative images and alt=\"\" for images that are purely decorative.", "alt=\"\""],
  "html.media.figure": ["Figure groups self-contained media or code with an optional figcaption that names or explains that content.", "Not every image needs figure, and figcaption does not replace required image alternatives when the image itself conveys information.", "A code screenshot and its explanatory caption are wrapped in figure while the image still has suitable alt text.", "Using figcaption means the nested image can omit its alt attribute.", "Use figure for self-contained referenced content, but still provide the nested media's required alternative text.", "required alternative text"],
  "html.media.responsive": ["srcset, sizes, and picture let the browser choose an appropriate image resource for density, viewport, format, or art direction.", "Incorrect sizes estimates waste bandwidth, art direction differs from resolution switching, and every candidate still needs stable dimensions and alternatives.", "An img supplies width descriptors and an accurate sizes rule so a narrow layout downloads an appropriately sized source.", "CSS width alone guarantees the browser downloads the smallest suitable image file.", "Provide truthful srcset and sizes metadata; CSS layout size alone does not tell resource selection which file to fetch.", "srcset and sizes"],
  "html.media.audio-video": ["Native audio and video controls provide playback semantics, while captions, transcripts, and audio description address different access needs.", "Autoplay can be blocked or harmful, captions are not the same as transcripts, and custom controls need complete keyboard and name/state support.", "A lesson video includes native controls, synchronized captions, and a linked transcript.", "Adding controls makes spoken video content accessible without captions or transcript.", "Provide controls plus synchronized captions for speech and a transcript or other alternatives appropriate to the media.", "synchronized captions"],
  "html.tables.structure": ["Data tables use table, row groups, rows, header cells, and data cells to preserve two-dimensional relationships.", "Tables should not be used for page layout, and visual alignment alone does not expose row and column relationships.", "A score table places column headings in thead and one learner record in each tbody row.", "Div elements styled as a grid automatically expose the same relationships as a table.", "Use native table structure for tabular data so row and column relationships remain programmatically available.", "tabular data"],
  "html.tables.headers": ["th cells and scope associate simple row or column headers with data cells so values have context outside visual position.", "Multi-level or irregular tables may need explicit id and headers associations, and scope must match actual structure.", "Each score column uses th scope=col and each learner name uses th scope=row.", "Bold text in the first row is enough for assistive technology to infer table headers.", "Use th with correct scope for simple tables and explicit associations when header relationships are complex.", "correct scope"],
  "html.tables.caption": ["A table caption gives the table an accessible name or concise purpose and appears as the table's own label.", "A nearby heading can add context but does not always replace caption semantics, and captions should not repeat every cell.", "The caption Quarterly support response times distinguishes the table from two adjacent metrics tables.", "The title attribute is the preferred accessible label for every table.", "Use caption to name a data table concisely and use surrounding text for additional explanation.", "caption"],
  "html.tables.complexity": ["Complex tables should be simplified or split when possible; otherwise explicit header associations and linear reading checks must preserve meaning.", "Merged cells, nested headers, scrolling, and responsive transformations can destroy relationships even when the desktop view looks clear.", "A two-tier financial header uses unique ids and headers attributes only after a simpler table design is found insufficient.", "More merged header cells always make a table easier to understand.", "Prefer a simpler table, and when complexity is necessary, define and test explicit header associations and reading order.", "explicit header associations"],
  "html.forms.form": ["A form groups controls for submission, with action, method, successful control names, and encoding determining the request.", "GET and POST have different semantics, disabled controls are not successful, and client validation does not secure server processing.", "A search form uses GET so its named query appears in a bookmarkable URL.", "The form element automatically sends every visible control even when it lacks a name.", "Only successful named controls are submitted; choose method and action from request semantics and validate again on the server.", "named controls"],
  "html.forms.labels-controls": ["A label associates an accessible name with its form control through nesting or matching for and id values.", "Placeholder text is not a persistent label, duplicate ids break association, and custom controls need equivalent semantics and state.", "A label for=email points to one input id=email while help and errors are connected separately.", "Placeholder text is an adequate replacement for a visible label.", "Give every control a persistent programmatic label; placeholders may offer examples but do not replace labels.", "programmatic label"],
  "html.forms.groups": ["fieldset and legend name a related group of controls, especially radio choices sharing one question.", "Visual borders are optional, nested groups need clarity, and a heading alone may not create the same form-control group relationship.", "A delivery-method fieldset uses legend Delivery method and labels each radio option.", "Radio buttons with the same name never need a group label.", "Use fieldset and legend to expose the shared question for a related control group.", "fieldset and legend"],
  "html.forms.validation": ["Native constraints such as required, type, min, max, and pattern provide browser validation semantics and should be paired with clear instructions and errors.", "Client validation is bypassable, pattern syntax and locale need care, and invalid styling alone cannot communicate an error.", "An email input is labeled, required, and reports a text error linked to the control while the server repeats validation.", "The required attribute guarantees valid and trustworthy data reaches the server.", "Use native constraints for user feedback, then perform authoritative validation on the server and communicate errors in text.", "authoritative validation on the server"],
  "html.metadata.title-description": ["A unique concise title identifies the page and current task; a meta description can summarize content for search presentation but is not visible page labeling.", "Search engines can ignore descriptions, title updates matter in client navigation, and duplicate titles make tabs and history ambiguous.", "The lesson page title reads Variables in C - Codestead rather than the same Home title used everywhere.", "The meta description is the page's accessible heading and replaces an h1.", "Provide a unique document title and visible heading; metadata descriptions do not replace in-page structure.", "unique document title"],
  "html.metadata.resources": ["Stylesheets, scripts, icons, and preload hints declare external resources with loading behavior that can affect rendering, execution order, security, and performance.", "Preload can waste bandwidth, blocking scripts delay parsing, integrity and CORS interact, and module scripts follow different dependency behavior.", "A noncritical module script loads with module semantics while the main stylesheet remains a normal render dependency.", "Adding preload to every resource always improves page speed.", "Declare only measured critical resource hints and understand each resource's fetch and execution behavior.", "measured critical resource hints"],
  "html.accessibility.native": ["Native HTML controls provide built-in keyboard, focus, role, name, state, and form behavior that custom widgets must recreate completely.", "Native semantics still require labels and correct use, and ARIA can supplement but cannot repair invalid interaction design automatically.", "A button element triggers an action and works with Enter and Space without a custom keydown implementation.", "A clickable div with role=button is fully equivalent to button without any other code.", "Prefer the native button for actions because role alone does not add keyboard, focus, disabled, or form behavior.", "native button"],
  "html.accessibility.audit": ["An HTML accessibility audit combines DOM and accessibility-tree inspection, keyboard use, names and states, zoom, content review, and automated checks.", "Automated tools detect only a subset, passing rules is not proof, and screen-reader testing needs task-focused interpretation.", "A learner completes a form using only keyboard, inspects labels and errors, then uses an automated scanner to find additional issues.", "A zero-error automated accessibility report certifies that the page is accessible.", "Treat automation as bounded evidence and combine it with keyboard, structure, name/state, visual, and task-based human checks.", "bounded evidence"],
  "html.quality.conformance": ["Conformance validation finds markup that violates HTML parsing or content-model requirements before browser recovery hides defects.", "A conforming document can still be inaccessible or misleading, and embedded templates or frameworks can require validating rendered output too.", "A validator identifies a button nested inside a link, and the structure is redesigned instead of relying on browser repair.", "If a page looks correct in one browser, HTML conformance errors cannot matter.", "Validate authored and rendered HTML, fix structural errors, and still perform accessibility and content review afterward.", "rendered HTML"],
  "html.quality.content-review": ["Content review checks headings, link purpose, plain language, alternatives, instructions, errors, and reading order against real user tasks.", "Correct syntax cannot guarantee comprehensible wording, and accessibility depends on language and context as well as elements.", "A reviewer replaces three Learn more links with destination-specific names and simplifies an unexplained acronym.", "Semantic elements automatically make the words inside them clear and accessible.", "Review the actual language and task context; semantic markup supports but does not create understandable content.", "actual language"],
  "html.quality.progressive": ["Progressive enhancement begins with meaningful HTML and core actions, then adds CSS and JavaScript without removing baseline content or navigation.", "Some applications require scripting, but loading, failure, and unsupported states still need intentional communication and recovery.", "A server-rendered course list remains readable and linked before JavaScript adds filtering and saved progress.", "Progressive enhancement means every advanced interaction must work identically with JavaScript disabled.", "Start from meaningful resilient HTML and define honest fallbacks or failure states for enhancements that require scripting.", "resilient HTML"],
  "html.project.site": ["A small site needs consistent document structure, navigation, titles, landmarks, valid links, accessible media and forms, and a repeatable review checklist.", "Page-by-page success can hide cross-page inconsistency, broken deployment paths, duplicate ids, stale content, or missing error states.", "A portfolio site tests every navigation path from its deployment subdirectory and audits keyboard flow on each template.", "If each page validates alone, the complete site's navigation and user journeys are automatically correct.", "Verify the complete deployed site, including cross-page navigation, paths, consistency, content, and task flows.", "complete deployed site"],

  "css.cascade.rules": ["A CSS declaration participates in the cascade only after its selector matches and its property value parses, then winning declarations produce computed values.", "Invalid values are discarded, shorthands reset subproperties, and animations or transitions can temporarily affect values.", "A misspelled display value is ignored, so an earlier valid display declaration remains effective.", "The last textual declaration always wins even when its selector does not match.", "A declaration must match and be valid before cascade order matters; inspect the winning declaration and computed value.", "winning declaration"],
  "css.cascade.sources": ["Cascade origin, importance, layer, specificity, scoping proximity, and source order resolve competing declarations in a defined priority sequence.", "Transitions and animations have special positions, user important styles protect preferences, and layers reorder author rules before specificity.", "A low-specificity rule in a later author layer can beat a highly specific rule in an earlier layer.", "Specificity is always the first and strongest cascade factor.", "Compare origin, importance, layer, specificity, and order in sequence; specificity is not the first universal factor.", "importance, layer, specificity"],
  "css.cascade.specificity": ["Selector specificity compares id, class-like, and type-like components lexicographically after higher cascade factors tie.", "The :where() pseudo-class contributes zero specificity, :is() uses its most specific argument, and inline styles have their own position.", "A component class beats a type selector without needing an id or !important escalation.", "Specificity is one total number where ten classes always equal one id.", "Treat specificity as compared components, not base-ten arithmetic, and keep component selectors intentionally low.", "compared components"],
  "css.cascade.inheritance": ["Some computed properties inherit from the parent by default, while others use their initial value unless explicitly inherited.", "Inheritance differs from the cascade, custom properties inherit by default, and form controls can have user-agent styles.", "Setting color on an article reaches ordinary descendant text, but margin does not automatically propagate.", "Every CSS property applied to a parent automatically applies to all descendants.", "Check each property's inheritance behavior; inheritable text properties differ from non-inherited box and layout properties.", "inheritance behavior"],
  "css.selectors.core": ["Selectors match elements by type, class, id, attribute, and relationship, and combinators express ancestry or sibling structure.", "Overly structural selectors break when markup changes, ids create high specificity, and selector matching does not alter DOM structure.", "A .card__title class targets the component role without depending on three wrapper levels.", "A selector describes how the browser should create or rearrange HTML elements.", "Selectors match existing elements; use stable semantic classes and relationships without coupling to unnecessary wrapper depth.", "match existing elements"],
  "css.selectors.states": ["Pseudo-classes select state or structure such as :focus-visible, :checked, :disabled, and :nth-child, while pseudo-elements represent generated boxes.", "Hover is unavailable to many users, generated content may not be reliably exposed, and structural indexes count actual sibling rules.", "A control has visible :focus-visible styling independent of its hover styling.", "If an interface works on :hover, it automatically works for keyboard and touch users.", "Design hover, focus, active, disabled, and touch behavior separately, with a visible :focus-visible state.", ":focus-visible"],
  "css.system.custom-properties": ["Custom properties store cascading token values that are substituted at computed-value time and can vary by scope or theme.", "They are not preprocessor variables, can become invalid at use time, inherit by default, and need fallback design.", "A --color-text token changes inside a dark theme scope and every consuming component receives the scoped value.", "Custom properties are globally fixed constants evaluated before the cascade.", "Custom properties participate in the cascade and scope; define meaningful fallbacks and validate the property where var() is used.", "participate in the cascade"],
  "css.system.organization": ["A maintainable CSS system separates tokens, base rules, layout primitives, components, utilities, and controlled overrides with documented cascade layers.", "Naming alone cannot prevent leakage, excessive utility combinations obscure intent, and global resets can affect third-party content.", "The project orders reset, base, components, and utilities in layers so overrides do not depend on import accidents.", "Adding !important wherever a conflict appears is a scalable organization strategy.", "Define ownership and cascade layers, keep specificity low, and reserve important rules for explicit policy rather than conflict repair.", "cascade layers"],
  "css.box.model": ["Each box has content, padding, border, and margin; box-sizing controls whether declared dimensions include padding and border.", "Margins can collapse in block flow, inline boxes differ, and intrinsic sizing or min-size constraints can override naive arithmetic.", "With border-box, a 200px width includes horizontal padding and border instead of producing a wider outer border box.", "Declared width always equals the final outer width of an element.", "Inspect box-sizing and every box edge; width does not necessarily equal the final outer size.", "box-sizing"],
  "css.box.units": ["CSS units express absolute, font-relative, viewport, container, and percentage relationships whose reference depends on the property.", "Viewport units can interact with browser UI, percentages resolve differently by property, and rem does not follow component-local font size.", "Body text uses rem for user-scalable sizing while a border uses px for a stable one-device-pixel-like design choice.", "rem and em always resolve to the same font-size reference.", "rem references the root font size while em depends on the relevant element font size or property context.", "root font size"],
  "css.box.intrinsic": ["Intrinsic sizing uses min-content, max-content, fit-content, aspect ratio, and content contributions to negotiate available space.", "Long unbreakable content, replaced elements, min-width auto, and writing mode can defeat fixed-layout assumptions.", "A grid track uses minmax(0,1fr) so long content may shrink instead of forcing the entire layout wider.", "A 1fr track always shrinks below its content's minimum without any additional rule.", "Account for intrinsic minimums; use an explicit zero minimum when a flexible track must be allowed to shrink.", "intrinsic minimums"],
  "css.box.overflow": ["Overflow controls clipping and scroll containers when content exceeds a box, while wrapping and min-size choices determine whether overflow occurs.", "Hidden content can become inaccessible, scrollbars affect layout, focus can move into clipped regions, and two-axis scrolling harms reading.", "A code sample uses overflow:auto and remains keyboard-scrollable without clipping long lines permanently.", "overflow:hidden is a safe universal fix for any layout that becomes too large.", "Resolve the sizing cause first and use accessible scrolling where content must remain available; do not hide meaningful overflow blindly.", "accessible scrolling"],
  "css.type.fonts": ["Font-family lists provide ordered fallbacks, web fonts need loading strategy, and font metrics affect wrapping, layout shift, and readability.", "A downloaded font can fail, synthetic styles differ, variable axes need ranges, and icon fonts can expose incorrect text.", "A system fallback renders immediately while the chosen web font loads with compatible metrics.", "Declaring one custom font guarantees it loads before any text is displayed.", "Provide appropriate fallback fonts and a measured font-display strategy because web-font loading can fail or arrive late.", "fallback fonts"],
  "css.type.readability": ["Readable typography balances line length, line height, spacing, alignment, hierarchy, and user zoom without fixing text into inflexible boxes.", "Optimal values vary by script, language, content, and font; user overrides and 200 percent zoom must remain usable.", "Article text uses a bounded measure and unitless line-height while containers expand for longer translated labels.", "Smaller text always fits more content and therefore improves usability.", "Prioritize legibility and reflow with scalable text, adequate line height, and layouts that survive zoom and longer content.", "survive zoom"],
  "css.color.contrast": ["Text and meaningful graphical objects need contrast against their actual backgrounds across default, hover, focus, disabled, and theme states.", "Transparency, gradients, images, thin fonts, antialiasing, and user themes can change effective contrast.", "A focus indicator and error text are measured on every background they can overlap, not only the design canvas.", "A contrast-safe text hex value remains compliant on every translucent or image background.", "Measure the rendered foreground-background pair in each state; a color value alone has no contrast ratio.", "foreground-background pair"],
  "css.visual.backgrounds": ["Background layers paint behind content and can combine color, images, gradients, size, position, and repeat without adding semantic content.", "Background images can fail, disappear in forced colors, increase contrast problems, and should not carry indispensable information.", "A decorative gradient sits over a solid fallback color while all instructional text remains real HTML.", "A background image is an appropriate place for required instructions because it cannot be selected.", "Keep essential information in semantic content and use backgrounds decoratively with resilient color and contrast fallbacks.", "semantic content"],
  "css.layout.flow": ["Normal flow lays out block and inline content in writing-mode order and provides the resilient baseline before specialized layout.", "Floats, out-of-flow positioning, fragmentation, margin collapse, and replaced elements can alter expected flow.", "A document remains readable in source order before grid enhances the card arrangement.", "Modern layouts should remove every element from normal flow for precise control.", "Preserve meaningful source and normal flow, then add layout without breaking reading order or content expansion.", "normal flow"],
  "css.layout.position": ["Positioned layout uses static, relative, absolute, fixed, or sticky positioning and a containing block that determines offsets.", "Transforms can create containing blocks, sticky needs a scroll context and inset, and out-of-flow elements do not reserve ordinary space.", "A badge is absolutely positioned inside a relatively positioned card whose padding reserves visual room.", "position:absolute always uses the viewport as its coordinate system.", "Determine the containing block first; absolute offsets are not automatically relative to the viewport.", "containing block"],
  "css.layout.stacking": ["Painting order and stacking contexts determine overlap; z-index compares boxes within the same relevant stacking context rather than globally.", "Opacity, transforms, positioned elements, isolation, and top-layer features can create contexts that trap high z-index descendants.", "A tooltip remains under a modal because its ancestor stacking context is below the modal, despite a large child z-index.", "The largest z-index anywhere in the document always appears on top.", "Inspect stacking-context ancestry; z-index values only compete within the applicable context.", "stacking-context ancestry"],
  "css.layout.logical": ["Logical properties express block and inline dimensions or edges so layout adapts to writing mode and text direction.", "Some visuals remain physically oriented, mixed-direction content needs testing, and source order still determines navigation.", "A card uses margin-inline-start instead of margin-left so spacing mirrors in right-to-left text.", "RTL support is complete when text-align is switched to right.", "Use logical properties and test direction, writing mode, content order, icons, and interactions rather than flipping alignment alone.", "logical properties"],
  "css.flex.axes": ["Flex layout distributes items along a main axis and aligns them along a cross axis chosen by flex-direction and writing mode.", "Row is not always physical horizontal, wrapping creates multiple lines, and order changes visual placement without changing DOM order.", "A column flex container uses justify-content for vertical distribution and align-items for horizontal cross-axis alignment.", "justify-content always controls horizontal alignment.", "Name the current main and cross axes first; justify-content acts on the main axis, not a fixed physical direction.", "main axis"],
  "css.flex.alignment": ["Flex sizing combines basis, grow, shrink, intrinsic minimums, gaps, and alignment to distribute available or negative space.", "The default min-size can prevent shrinking, auto margins absorb space, and align-content affects lines rather than a single line.", "A text item gets min-width:0 so it can shrink beside a fixed icon without overflowing.", "flex:1 guarantees equal final widths regardless of content and minimum size.", "Account for basis and intrinsic minimums; flex growth alone does not guarantee equal final sizes.", "intrinsic minimums"],
  "css.grid.tracks": ["Grid defines explicit or implicit rows and columns, places items into grid areas, and sizes tracks with fixed, intrinsic, flexible, and minmax functions.", "Implicit tracks can appear unexpectedly, fr shares leftover space after minimums, and dense placement can change visual order.", "A card grid uses repeat(auto-fit,minmax(min(100%,18rem),1fr)) to create bounded responsive columns.", "1fr means one equal fraction of the entire container before content minimums are considered.", "Flexible fr tracks share leftover space after track minimums and other sizing contributions are resolved.", "leftover space"],
  "css.grid.responsive": ["Responsive grid changes track count and size from available space and content needs rather than duplicating markup for each viewport.", "Auto-fit and auto-fill differ for empty tracks, fixed minimums can overflow, and visual rearrangement must not contradict focus order.", "A gallery collapses naturally to one column when its minimum card size no longer fits twice.", "A responsive grid requires a media query for every possible device width.", "Use content-aware track functions where suitable and add breakpoints only when the layout itself needs a structural change.", "content-aware track functions"],
  "css.responsive.media": ["Media queries condition styles on media features such as width, orientation, pointer, hover, contrast, color scheme, and motion preference.", "Device labels are unreliable, viewport width can change, feature combinations matter, and queries do not change DOM semantics.", "Navigation changes layout when its content no longer fits, not at a guessed tablet model width.", "Media queries should target named devices such as iPhone and desktop.", "Choose breakpoints from content and capabilities, not device brands, and test resize, zoom, input, and preference combinations.", "content and capabilities"],
  "css.responsive.container": ["Container queries style a component from an ancestor query container's available size or style, enabling context-independent composition.", "A query container must be established, containment affects sizing, and viewport concerns still need media queries.", "The same card switches to a side-by-side layout only when its panel container is wide enough.", "Container queries measure the component's own final size without any configured ancestor.", "Define the intended query container and style from that container's conditions, not an assumed page viewport.", "query container"],
  "css.motion.transitions": ["Transitions interpolate animatable property values when computed values change, using declared duration, delay, and easing.", "Display does not generally interpolate like opacity, layout properties can be expensive, and motion must not hide state or block interaction.", "A disclosure icon rotates briefly when aria-expanded changes while the content state updates immediately.", "transition: all is the safest way to animate every visual change consistently.", "Transition only deliberate properties with measured timing; avoid transition:all and keep state understandable without motion.", "deliberate properties"],
  "css.motion.preferences": ["prefers-reduced-motion lets authors reduce or replace nonessential movement while preserving content, state change, and control.", "Reduced does not always mean zero, vestibular triggers vary, and autoplay or canvas motion may need JavaScript coordination.", "A reduced-motion query removes parallax and uses an instant opacity state change for the same revealed content.", "Reduced-motion users should receive no state feedback because every transition must be removed.", "Preserve equivalent state feedback while removing or minimizing nonessential spatial and continuous motion.", "equivalent state feedback"],
  "css.quality.devtools": ["CSS debugging compares matched rules, cascade priority, computed values, box geometry, layout overlays, and responsive states before editing.", "DevTools can show a simulated state that differs from real input, and local edits disappear unless transferred to source.", "A learner finds overflow by inspecting the grid track minimum rather than adding random widths.", "The Styles panel shows only declarations that currently win, so crossed-out rules can be ignored.", "Inspect matched and overridden rules plus computed values; a crossed-out declaration often explains the competing cascade.", "computed values"],
  "css.quality.compatibility": ["Compatibility decisions use the declared browser matrix, current feature support, progressive enhancement, and tested fallbacks rather than folklore.", "Support data can vary by subfeature, prefixes may be obsolete, and transpilation cannot emulate every layout or platform behavior.", "A new layout feature is guarded with @supports and the normal-flow fallback remains usable.", "Adding every vendor prefix found online guarantees modern cross-browser compatibility.", "Use current support data and real matrix testing, add targeted fallbacks, and avoid obsolete cargo-cult prefixes.", "real matrix testing"],
  "css.quality.accessibility": ["Accessible CSS preserves visible focus, zoom and reflow, contrast, target size, reading order, user preferences, and content visibility.", "Visual hiding methods differ, generated content may be unavailable, and reordering can separate visual from DOM navigation.", "At 200 percent zoom a form reflows without two-dimensional scrolling and focus remains visible.", "Accessibility is handled entirely by semantic HTML, so CSS cannot create barriers.", "Test CSS-driven focus, contrast, reflow, order, motion, and visibility because presentation can create or remove access barriers.", "presentation can create or remove access barriers"],
  "css.project.system": ["A CSS project system combines documented tokens, layers, primitives, components, responsive rules, states, and visual regression or manual checks.", "A token catalog can still be inconsistent, component variants can explode, and global rules can leak across independently authored pages.", "A small site reuses spacing and color tokens while each component demonstrates default, focus, error, long-content, and narrow states.", "A design system is complete once colors and spacing variables are defined.", "A CSS system also needs ownership, component contracts, state coverage, responsive behavior, accessibility checks, and change governance.", "component contracts"],
  "javascript.runtime.script": ["Classic and module scripts execute JavaScript in browser realms with different scope, loading, and strictness rules.", "Parser blocking, async, defer, module dependencies, and CSP change timing and availability.", "A deferred classic script runs after parsing while preserving document order.", "Every script tag executes immediately before the browser parses later HTML.", "Choose explicit script loading semantics; defer waits for parsing, while async can execute as soon as fetched.", "defer waits for parsing"],
  "javascript.runtime.modules": ["ECMAScript modules have explicit imports and exports, per-module scope, strict semantics, and a dependency graph evaluated once.", "Live bindings are not copied values, cycles need careful initialization, and browser imports require resolvable URLs.", "A named export is imported by exact name and remains linked to the exporting module binding.", "Importing a value copies it permanently and each import re-executes the module.", "Module imports are live bindings and a module is evaluated once per realm after dependency linking.", "live bindings"],
  "javascript.runtime.devtools": ["Browser DevTools expose sources, breakpoints, network, console, DOM, storage, and performance evidence for one runtime state.", "Console evaluation can mutate state, source maps can mislead, and local overrides are not persisted automatically.", "A breakpoint confirms an event payload before code is changed and a regression test preserves the finding.", "The console is only for printing and cannot alter application behavior.", "Treat DevTools as an observation environment that can also mutate state; verify changes in source and tests.", "can also mutate state"],
  "javascript.runtime.event-loop": ["The event loop runs one task at a time, drains microtasks at defined checkpoints, and lets rendering occur between suitable tasks.", "Long tasks block interaction, promise callbacks are microtasks, and timers specify minimum delay rather than exact execution time.", "A resolved Promise callback runs after current synchronous code but before a zero-delay timer task.", "setTimeout(fn,0) runs fn immediately before queued Promise callbacks.", "Current synchronous work finishes first, then microtasks drain before the next timer task.", "microtasks drain"],
  "javascript.values.primitives": ["JavaScript primitives are immutable values including undefined, null, Boolean, number, bigint, string, and symbol.", "Wrapper objects differ from primitives, number has NaN and infinities, and bigint does not mix implicitly with number arithmetic.", "Changing one character creates a new string value rather than mutating the existing primitive.", "Strings are mutable arrays because bracket access returns characters.", "String primitives are immutable; operations return new values rather than changing characters in place.", "immutable"],
  "javascript.values.variables": ["let and const create block-scoped bindings, while const prevents rebinding but does not freeze referenced objects.", "Temporal dead zones, var function scope, global bindings, and closure capture affect visibility and lifetime.", "A const array can receive push because its binding stays fixed while the array object mutates.", "const makes every nested property of an object immutable.", "const prevents rebinding; use freezing or immutable updates when object mutation must also be prevented.", "prevents rebinding"],
  "javascript.values.coercion": ["JavaScript operators can perform explicit or implicit conversion according to operator-specific abstract operations.", "Equality, addition, truthiness, property keys, and relational comparison use different coercion paths.", "Number('12') explicitly produces 12 while '12' + 3 concatenates to '123'.", "The plus operator always performs numeric addition when one operand is a number.", "String participation can make + concatenate; convert inputs explicitly before arithmetic.", "convert inputs explicitly"],
  "javascript.values.numbers": ["JavaScript number uses IEEE-754 binary floating point, so safe integers and decimal fractions have finite precision limits.", "NaN is unequal to itself, -0 exists, and bigint is a separate exact-integer type.", "0.1 + 0.2 is compared with a tolerance instead of exact equality to 0.3.", "All decimal arithmetic is exact because number displays familiar decimal text.", "Binary floating-point decimals can round; use tolerance or domain-appropriate decimal representation.", "can round"],
  "javascript.control.selection": ["if, switch, and conditional expressions choose paths using truthiness or strict case matching.", "Falsy values include 0, empty string, null, undefined, false, and NaN, while empty arrays are truthy.", "A nullish check distinguishes missing count from a legitimate zero.", "An empty array is falsy because it contains no elements.", "Arrays and objects are truthy even when empty; test length or the intended condition explicitly.", "truthy even when empty"],
  "javascript.control.iteration": ["JavaScript offers index, iterable, property, and array-method iteration with different keys, values, mutation, and async behavior.", "for...in includes enumerable property names, for...of needs an iterable, and async callbacks in forEach are not awaited collectively.", "for...of visits array values while an index loop is used when positions are required.", "for...in is the preferred way to iterate array values.", "Use for...of for iterable values; for...in enumerates property keys and is unsuitable as ordinary array-value traversal.", "for...of"],
  "javascript.functions.forms": ["Function declarations, expressions, arrows, methods, and classes differ in hoisting, this, arguments, constructibility, and naming.", "Arrow functions capture lexical this and cannot be constructors, while methods receive this from the call form.", "An arrow callback reads surrounding this intentionally without rebinding it.", "Arrow functions are shorter syntax for every function and can always replace methods or constructors.", "Arrow functions have lexical this and are not constructible; choose a form from call semantics, not length.", "lexical this"],
  "javascript.functions.parameters": ["Parameters receive argument values and can use defaults, rest collection, and destructuring with explicit validation.", "Defaults apply only to undefined, objects remain shared references, and destructuring can throw on nullish input.", "A function uses options = {} before destructuring optional properties.", "A default parameter runs for null and every other falsy argument.", "Parameter defaults apply when the argument is undefined, not for null or all falsy values.", "argument is undefined"],
  "javascript.collections.arrays": ["Arrays are ordered objects with a length and integer-indexed properties, supporting mutation and nonmutating transformations.", "Sparse holes, length assignment, reference identity, and in-place methods can surprise callers.", "slice returns a shallow copied range while splice mutates the source array.", "slice and splice are interchangeable ways to copy an array section.", "slice is nonmutating, while splice mutates elements and length; choose from the ownership contract.", "splice mutates"],
  "javascript.collections.methods": ["Array methods such as map, filter, reduce, find, some, and every encode distinct transformation and query contracts.", "Callbacks can have side effects, reduce needs a sound accumulator, and map does not flatten arbitrary nested arrays.", "filter retains matching values and map transforms each retained value in a separate step.", "map should be used for side effects when its returned array is ignored.", "Use map when its transformed result is needed; use iteration for deliberate side effects.", "transformed result"],
  "javascript.collections.objects": ["Objects associate property keys with values through own and inherited properties, descriptors, and prototype lookup.", "Enumeration order has rules, symbols are separate keys, spreading is shallow, and untrusted keys can affect prototypes.", "Object.hasOwn distinguishes an own configuration key from one inherited through the prototype chain.", "The in operator checks only an object's own properties.", "The in operator includes inherited properties; use Object.hasOwn when only direct ownership is valid.", "includes inherited properties"],
  "javascript.collections.map-set": ["Map stores key-value associations using arbitrary identity keys, while Set stores unique values using SameValueZero equality.", "Object and Map have different key semantics, iteration order is insertion order, and object keys compare by identity.", "A Map uses a DOM node object as a key without string coercion.", "Two separate objects with identical properties are one Set value because their contents match.", "Object keys use identity in Map and Set; structurally equal but distinct objects remain distinct entries.", "use identity"],
  "javascript.objects.this": ["JavaScript this is determined by call form, except arrows capture it lexically; extracting a method can lose its receiver.", "Strict mode, constructors, bind, event callbacks, and modules affect the resulting value.", "Calling user.getName() supplies user as receiver, but assigning the method to a variable removes that call-site receiver.", "this is permanently defined where a normal function is written.", "For normal functions, this comes from the call site; bind it or preserve the receiver when extracting methods.", "call site"],
  "javascript.objects.prototype": ["Objects delegate missing property lookup through a prototype chain, and constructor prototype properties can be shared by instances.", "Shadowing, mutation of shared prototypes, null prototypes, and pollution from untrusted keys require care.", "An instance method is found on the class prototype while instance data remains an own property.", "Every object contains a private copied version of all prototype methods.", "Prototype methods are shared and found by delegated lookup, not copied into every instance.", "delegated lookup"],
  "javascript.objects.classes": ["JavaScript class syntax defines constructor and prototype methods with private fields and inheritance rules over the existing object model.", "Classes are strict, methods are not auto-bound, private names are lexical, and derived constructors must initialize super first.", "A Counter private field changes only through methods that preserve its nonnegative invariant.", "Class methods automatically retain their instance this when passed as callbacks.", "Methods are not auto-bound; preserve the receiver, bind explicitly, or use an intentional instance arrow field.", "not auto-bound"],
  "javascript.objects.composition": ["Object composition combines focused functions or components through explicit interfaces instead of deep inheritance chains.", "Shared mutable state and ad-hoc mixins can still create coupling or name collisions.", "A logger and storage adapter are passed into a service rather than inherited from one base class.", "Inheritance is required whenever two objects share any behavior.", "Prefer composition for has-a collaboration and inject narrow capabilities instead of inheriting solely for reuse.", "has-a collaboration"],
  "javascript.scope.lexical": ["Lexical scope resolves names from nested source structure, with block scope for let and const and function scope for var.", "Temporal dead zones, shadowing, modules, eval, and closures affect availability.", "An inner block shadows outer count without changing it after the block ends.", "JavaScript resolves a variable from whichever caller function invoked the current function.", "JavaScript uses lexical scope: name resolution follows where functions and blocks are defined, not their caller.", "lexical scope"],
  "javascript.scope.closures": ["A closure retains access to lexical bindings after the creating scope returns, observing the binding's current value.", "Captured objects can prolong memory, loop binding choice matters, and stale state can arise in framework callbacks.", "A counter factory returns a function that updates its private count binding across calls.", "A closure stores a frozen copy of every captured primitive at creation time.", "Closures retain bindings, not automatic frozen snapshots; later updates to the binding can be observed.", "retain bindings"],
  "javascript.modules.import-export": ["Named and default exports form explicit module interfaces, and imports are statically analyzable live bindings.", "Default naming is local, re-exports can cause cycles, and dynamic import returns a promise.", "A utility module exports parse and format by name so callers import only the declared interface.", "A module may have several default exports with different names.", "A module has at most one default export; use named exports for multiple explicit interface members.", "one default export"],
  "javascript.modules.architecture": ["Module architecture groups cohesive responsibilities, keeps dependency direction clear, and isolates browser, network, and storage boundaries.", "Tiny files can create cycles, barrel exports can hide coupling, and shared mutable singletons undermine tests.", "A domain module accepts a storage port instead of importing localStorage throughout business logic.", "More modules always mean lower coupling regardless of dependency direction.", "Choose cohesive module boundaries and enforce dependency direction; file count alone does not reduce coupling.", "dependency direction"],
  "javascript.dom.query-update": ["DOM queries select existing nodes and updates change properties, attributes, classes, or text with semantics appropriate to the content.", "Queries can return null or live collections, innerHTML parses markup, and property and attribute values can differ.", "querySelector is null-checked before textContent updates trusted text without parsing HTML.", "innerHTML and textContent are interchangeable for displaying user text.", "Use textContent for text; innerHTML parses markup and is unsafe for untrusted strings without robust sanitization.", "textContent"],
  "javascript.dom.create": ["DOM creation builds elements, sets safe properties and text, then inserts fragments or nodes while preserving document semantics.", "Repeated insertion can move existing nodes, custom elements have lifecycle behavior, and large updates can cost layout work.", "A list item is created with document.createElement, receives textContent, and is appended to a fragment.", "Appending the same node twice creates two independent copies.", "Inserting an existing node moves that node; clone explicitly when a separate copy is required.", "moves that node"],
  "javascript.events.model": ["DOM events travel through capture, target, and bubble phases, with composed paths and default actions distinct from propagation.", "Not all events bubble, shadow DOM retargets, passive listeners restrict preventDefault, and stopPropagation does not cancel default action.", "One list listener uses event delegation and closest to handle buttons added later.", "preventDefault also stops the event from reaching ancestor listeners.", "preventDefault cancels a cancelable default action; propagation controls are separate.", "default action"],
  "javascript.events.forms": ["Form events should use submit at the form boundary, read named controls, validate, and preserve native keyboard submission.", "Click-only handling misses Enter submission, disabled fields are omitted, and async submission needs duplicate and error control.", "A submit listener prevents navigation only after validation and disables duplicate requests while showing status.", "Handling the submit button's click is equivalent to handling every form submission.", "Listen for the form submit event so keyboard and programmatic submissions follow the same path.", "submit event"],
  "javascript.async.promises": ["A Promise represents eventual fulfillment or rejection, and then creates a new promise whose result follows its handler.", "Errors can become unhandled, executors run synchronously, and Promise.all fails fast while work may continue.", "A parse step thrown inside then rejects the next promise and is handled by a terminal catch.", "then mutates and returns the same Promise object.", "then returns a new promise, enabling transformation and error propagation through a chain.", "new promise"],
  "javascript.async.await": ["async functions return promises, and await pauses only that async continuation while other tasks can proceed.", "Sequential awaits can serialize independent work, try/catch scope matters, and rejection still needs handling.", "Two independent fetch promises start before Promise.all is awaited, avoiding unnecessary serialization.", "await blocks the browser thread until the promise settles.", "await suspends the async function continuation, not the entire event loop.", "not the entire event loop"],
  "javascript.network.fetch": ["fetch resolves for HTTP error statuses and rejects mainly for network-level failures, so code checks response.ok before parsing.", "Bodies are one-use streams, parsing can fail, CORS controls access, and credentials and timeouts need explicit policy.", "A request checks response.ok, validates JSON shape, and presents separate HTTP, parse, and network errors.", "fetch rejects automatically for every 404 or 500 response.", "fetch can fulfill with an HTTP error response; check response.ok or status before treating it as success.", "response.ok"],
  "javascript.async.cancellation": ["AbortController communicates cancellation to APIs such as fetch, and consumers distinguish intentional abort from other failures.", "Abort is cooperative, not every operation supports the signal, and cancellation does not roll back completed side effects.", "A new search aborts the previous fetch and ignores its AbortError while reporting real network failures.", "Dropping a promise variable automatically cancels its underlying request.", "Promises do not cancel by being ignored; pass and trigger an AbortSignal to supported operations.", "AbortSignal"],
  "javascript.browser.url": ["URL and URLSearchParams parse and serialize addresses safely relative to an explicit base instead of manual string concatenation.", "Encoding, duplicate query keys, fragments, origin checks, and dangerous schemes require deliberate validation.", "new URL(userPath, location.origin) is checked for allowed origin and protocol before navigation.", "Concatenating '?' and raw user text always produces a valid encoded query.", "Use URLSearchParams for query encoding and validate protocol and origin before trusting constructed URLs.", "URLSearchParams"],
  "javascript.browser.storage": ["Web storage stores origin-scoped strings synchronously, while IndexedDB supports structured asynchronous storage; neither is a trusted secret vault.", "Quotas, eviction, private mode, cross-tab changes, serialization, and XSS exposure affect reliability.", "A preference is JSON-validated after localStorage read and falls back safely when missing or corrupt.", "localStorage is encrypted and safe for long-lived API keys because it belongs to one origin.", "Treat browser storage as script-readable and untrusted; do not store sensitive long-lived credentials there.", "script-readable"],
  "javascript.security.dom-xss": ["DOM XSS occurs when untrusted data reaches an HTML, URL, script, or style execution sink without context-appropriate handling.", "Escaping differs by context, sanitizers need policy and updates, and trusted types reduce but do not replace design.", "A comment is assigned with textContent rather than concatenated into innerHTML.", "Replacing angle brackets is enough to make untrusted text safe in every DOM sink.", "Keep untrusted data in textContent or safe typed APIs and avoid parsing it into executable markup.", "safe typed APIs"],
  "javascript.security.secrets-privacy": ["Browser JavaScript and delivered configuration are visible to users, so secrets must remain on trusted servers with minimized data collection.", "Source maps, logs, analytics, storage, URLs, and third-party scripts can expose data unexpectedly.", "A browser calls a server endpoint that holds the provider secret instead of embedding the secret in a bundle.", "An environment variable used during frontend build remains secret after bundling.", "Anything delivered to browser code is public to that user; keep secrets server-side and minimize exposed personal data.", "keep secrets server-side"],
  "javascript.errors.exceptions": ["Exceptions represent exceptional control transfer, and error handling should preserve cause, context, cleanup, and user-safe messaging.", "Catching too broadly hides defects, finally can override outcomes, and rejected promises require asynchronous handling.", "A parser throws a typed error with cause and the UI converts it into a bounded message while logs retain safe context.", "An empty catch block makes an operation robust by preventing crashes.", "Catch only where recovery or context is possible; do not silently swallow errors.", "do not silently swallow"],
  "javascript.quality.unit-tests": ["Unit tests verify deterministic module contracts with controlled inputs and dependencies, including boundaries and failures.", "Overmocking tests implementation, timers and randomness need control, and coverage is not correctness.", "A price function is tested at zero, threshold, normal, and invalid input without a browser.", "One happy-path assertion is enough when line coverage reaches 100 percent.", "Derive tests from contract partitions and boundaries; coverage alone does not establish behavior.", "contract partitions"],
  "javascript.quality.dom-tests": ["DOM tests should query by accessible role, name, label, or text and exercise realistic user events and state changes.", "jsdom differs from browsers, implementation selectors are brittle, and async updates require observable waiting.", "A test submits by button role and waits for an accessible status message instead of reading internal state.", "Tests should select generated class names because they identify exact implementation nodes.", "Query the interface as users perceive it and assert observable behavior, not generated classes or private state.", "users perceive it"],
  "javascript.project.browser-app": ["A browser app integrates modules, semantic UI, state, async boundaries, security, tests, build, and deployment paths into one traceable product flow.", "A working local happy path can hide offline, empty, error, stale, accessibility, privacy, and subpath failures.", "A small tracker handles loading, empty, success, validation, server failure, retry, keyboard, and narrow-screen states.", "Completing the primary success screen means a browser project is production-ready.", "Verify the complete state model, failure and recovery paths, accessibility, security, tests, and deployed base paths.", "complete state model"],

  "react.model.root": ["A React root owns rendering for one DOM container and reconciles component output into that managed subtree.", "Multiple roots do not share context automatically, hydration has stricter matching, and external DOM mutation can conflict.", "createRoot(container).render(<App />) gives React ownership of the app container without replacing unrelated page landmarks.", "Calling render appends a fresh duplicate DOM tree on every state change.", "React reconciles a root's managed subtree rather than blindly appending a new tree each render.", "managed subtree"],
  "react.model.project": ["A React project needs pinned React/build versions, one understood entry path, module boundaries, tests, and production build behavior.", "Development Strict Mode can repeat work, environment values can be public, and framework conventions differ.", "A Vite project renders under StrictMode, keeps browser-safe config explicit, and verifies a production build.", "If the development server works, dependency and production configuration are necessarily correct.", "Verify the pinned production build and runtime configuration; development success is not deployment evidence.", "production build"],
  "react.model.declarative": ["React components describe UI as a function of props, state, and context, and reconciliation updates the DOM to match the latest description.", "Render must stay pure, browser state can exist outside React, and imperative escape hatches need synchronization.", "Changing isOpen causes render to describe either the panel or its absence without manually inserting nodes.", "Declarative React means DOM events and state updates never occur imperatively.", "Describe UI from state and use event handlers to update state; reserve DOM imperative work for explicit escape hatches.", "UI from state"],
  "react.model.devtools": ["React DevTools inspect component trees, props, state, context, and profiling for a particular development render.", "Inspection can expose personal data, profiling changes timing, and observed rerenders are not automatically performance defects.", "A profiler shows an expensive list rerender before memoization is considered and measured again.", "Every highlighted rerender must be removed with memoization.", "Use DevTools measurements to find costly work; rerendering itself is normal and memoization needs evidence.", "measurements"],
  "react.components.functions": ["A function component receives props and returns renderable React nodes while remaining pure during render.", "Components can render null, hooks require stable call order, and local variables reset on each render.", "Greeting receives name and returns one heading without mutating props or external state.", "A component function runs once when mounted and retains its local variables forever.", "Component functions can run repeatedly; persist changing UI data in state or refs, not ordinary render locals.", "run repeatedly"],
  "react.components.jsx": ["JSX creates React element descriptions, embeds JavaScript expressions, and uses JavaScript property names and explicit escaping.", "It is not HTML, adjacent nodes need one parent, lowercase names mean host elements, and objects are not directly renderable children.", "A label uses htmlFor and a dynamic value is escaped when rendered as text.", "JSX strings inserted through braces are interpreted as raw HTML.", "React escapes ordinary JSX text expressions; raw HTML requires an explicit dangerous API and trusted sanitization.", "escapes"],
  "react.components.conditionals": ["Conditional rendering selects node descriptions with if, ternary, logical expressions, variables, or early returns.", "Falsy values such as zero can render unexpectedly with &&, and branch changes can preserve or reset state by identity.", "A count check uses count > 0 && so zero does not appear as stray content.", "value && <Panel/> always renders nothing for every falsy value without displaying that value.", "Logical AND can render zero; use an explicit Boolean condition when the left value is displayable.", "explicit Boolean condition"],
  "react.components.lists": ["React list keys identify sibling items across renders so reconciliation preserves the intended component and DOM state.", "Keys need only be unique among siblings, indexes fail for reordered editable lists, and keys are not passed as props.", "A todo list uses stable todo.id while sorting, insertion, and deletion preserve each row's input state.", "Array index is always the best key because it is unique during one render.", "Use stable data identity as key when items can reorder, insert, or delete; positional indexes can attach state to the wrong item.", "stable data identity"],
  "react.props.data": ["Props carry read-only data from parent to child and changes cause a new render description.", "Objects can still be mutated outside React, destructuring does not clone, and excessive prop drilling can signal boundary problems.", "A CourseCard receives title and progress and requests changes through a callback rather than editing props.", "A child may assign to props because they are ordinary JavaScript function parameters.", "Treat props as read-only inputs and ask the owning component to update state through an explicit callback.", "read-only inputs"],
  "react.props.children": ["children is a prop containing nested renderable content and enables wrapper, layout, and compound composition.", "Children may be absent, one node, or many; cloning them couples implementation and does not make ownership transfer.", "A Card renders its children inside semantic article structure without assuming their concrete type.", "children is always an array and can be indexed safely.", "children is an opaque renderable value; use supported React helpers only when traversal is actually required.", "opaque renderable value"],
  "react.props.interfaces": ["A component interface defines required and optional props, callbacks, controlled values, defaults, and semantic constraints.", "Type checking cannot prove accessible labels, callback effects, or valid combinations without deliberate design.", "A Button requires children and onPress while variant has a documented default and disabled behavior.", "TypeScript prop types automatically guarantee the component is accessible and semantically valid.", "Types check shapes; document and test semantic contracts, accessibility, state, and callback behavior separately.", "semantic contracts"],
  "react.props.purity": ["Pure render returns the same description for the same inputs and avoids mutation, subscriptions, network calls, and DOM writes.", "Time, randomness, module mutation, and id generation can make output unstable unless managed through supported mechanisms.", "A component computes filtered items from props during render without pushing into the original array.", "A fetch call during render runs once because React renders components only once.", "Render can repeat or be discarded; keep side effects out of render and synchronize external systems in events or effects.", "side effects out of render"],
  "react.state.use-state": ["useState stores component state between renders and its setter queues a replacement or updater for a later render.", "Updates batch, stale closures see prior snapshots, objects need replacement, and initializers can run more than once in development.", "Three increments use functional updates so each derives from the latest queued count.", "Calling setCount(count+1) three times always increases count by three.", "When next state depends on previous state, pass a functional updater to avoid stale snapshot calculations.", "functional updater"],
  "react.state.events": ["React event handlers respond to user actions and are the primary place to calculate state updates and invoke action-side effects.", "Handlers must be passed rather than called during render, async handlers capture snapshots, and propagation follows DOM semantics.", "onClick={save} passes the function while onClick={save()} would execute during render.", "Event handler props should call the function immediately so React knows what to do later.", "Pass a function to an event prop; invoking it during render performs the action at the wrong time.", "Pass a function"],
  "react.state.structure": ["State should be minimal, normalized, and free of redundant values that can be derived from props or other state during render.", "Caching expensive derivations differs from storing truth, nested mutation hides changes, and state shape controls update complexity.", "A cart stores items and derives total rather than storing an independently editable total that can drift.", "Every displayed value should have its own state variable.", "Store the minimal source of truth and derive dependent values to avoid inconsistent redundant state.", "source of truth"],
  "react.state.lifting": ["Shared state moves to the nearest common owner, which passes values and callbacks to coordinated children.", "Lifting too high increases coupling and rerenders, while independent state should remain local.", "Two temperature inputs share one converted value owned by their common calculator parent.", "Duplicating shared state in both children is safe if callbacks try to keep it synchronized.", "Use one common owner for genuinely shared state and pass controlled values rather than synchronizing duplicate copies.", "one common owner"],
  "react.forms.controlled": ["A controlled form field receives its current value from React state and reports edits through onChange.", "Every keystroke renders, nullish value transitions can switch control mode, and file inputs remain special.", "An input uses value={name} and updates name from event.target.value with a persistent label.", "Providing an initial defaultValue makes an input controlled forever.", "A controlled input uses value plus an update handler; defaultValue initializes an uncontrolled input.", "value plus an update handler"],
  "react.forms.validation": ["React form validation combines native constraints, controlled domain rules, submit handling, accessible errors, and authoritative server results.", "Validation timing can annoy users, async checks race, and client rules remain bypassable.", "Submit reveals linked text errors, focuses the first invalid field, and preserves server error messages safely.", "Disabling the submit button is enough to explain every invalid field.", "Expose field-specific text errors and server validation; disabled submission alone does not identify what to fix.", "field-specific text errors"],
  "react.state.reducer": ["useReducer centralizes related state transitions as pure actions from previous state to next state.", "Reducers cannot perform effects, actions need clear events, and a reducer is unnecessary for one simple independent value.", "A quiz reducer handles answer, next, reset, and finish while rejecting transitions after finish.", "A reducer should fetch data directly when processing a load action.", "Reducers stay pure; perform effects outside and dispatch actions representing their results.", "Reducers stay pure"],
  "react.state.reducer-context": ["Reducer plus context can share state and dispatch across a bounded subtree without threading every prop.", "One global context rerenders broad consumers, default values can hide missing providers, and server/client boundaries matter.", "A course editor provides state and dispatch only to its editing subtree and splits stable services from changing data.", "Context should replace all props and local state throughout an application.", "Use context for genuinely cross-cutting subtree data, keep local state local, and split contexts by change pattern.", "keep local state local"],
  "react.effects.need": ["An effect synchronizes React state with an external system after commit; derived render data and event consequences usually need no effect.", "Effects do not run on the server, can repeat in development, and set-state loops indicate derived-state misuse.", "A media-player effect calls play or pause to synchronize an external video element with isPlaying.", "Any calculation after state changes belongs in useEffect.", "Calculate render-derived values during render; use effects only to synchronize with systems outside React.", "systems outside React"],
  "react.effects.dependencies": ["Effect dependencies list every reactive value read so synchronization is reapplied when those values change.", "Object identity can retrigger effects, suppressing lint hides stale closures, and updater forms can remove a read dependency.", "A chat connection effect depends on roomId and serverUrl because both determine the external connection.", "An empty dependency array makes any effect safe to run once with permanently current values.", "Dependencies must match reactive reads; an empty array can capture stale initial values.", "reactive reads"],
  "react.effects.cleanup": ["An effect may return cleanup that reverses its subscription, timer, request, or connection before rerun and unmount.", "Cleanup must correspond to the same setup, async races need abort or ignore logic, and development can setup-cleanup-setup.", "A resize listener is removed with the exact same function reference when the effect cleans up.", "Cleanup runs only when the whole application closes, not before an effect reruns.", "React cleans up before a changed effect reruns and on unmount; write setup and cleanup as a reversible pair.", "reversible pair"],
  "react.refs.dom": ["A ref retains a mutable value without rendering and can point to a DOM node for focus, measurement, or imperative APIs.", "Changing ref.current does not render, DOM nodes can be null, and refs should not replace state for visible data.", "After a validation error, a ref focuses the first invalid input in a controlled event or effect.", "Updating ref.current automatically rerenders every component that reads it.", "Refs are mutable escape hatches and do not trigger rendering; use state for information that affects UI.", "do not trigger rendering"],
  "react.data.states": ["Data UI explicitly represents idle, loading, success, empty, stale, and error states rather than treating data as one nullable value.", "Previous data, background refresh, partial results, and permission failures can overlap these labels.", "A course list distinguishes loading skeleton, empty catalog, loaded rows, and retryable server error.", "An empty array during loading is equivalent to a confirmed empty result.", "Track loading and outcome separately so unknown data is not misrepresented as a confirmed empty state.", "not misrepresented"],
  "react.data.fetch": ["React data fetching needs ownership, cache and freshness policy, cancellation, race handling, and explicit UI states.", "Effects can duplicate requests, frameworks may fetch elsewhere, and later responses can overwrite newer intent.", "Changing learnerId aborts or ignores the prior request before its stale response can update current state.", "A fetch effect needs no cleanup because React ignores every response after props change.", "Cancel or ignore obsolete requests and prevent stale responses from updating state for newer inputs.", "stale responses"],
  "react.data.mutations": ["A mutation models pending, success, validation failure, conflict, retry, and cache reconciliation while preventing accidental duplicates.", "Optimistic updates need rollback, retries need idempotency, and server truth can reject client assumptions.", "A save button disables duplicate submission and merges the server-returned version after success.", "Optimistic UI means the server result can be ignored once local state changes.", "Reconcile mutations with authoritative server results and roll back or explain failed optimistic changes.", "authoritative server results"],
  "react.data.custom-hook": ["A custom hook packages reusable stateful logic and other hooks while each caller receives independent state unless an external store is shared.", "Hooks must follow call-order rules, names begin with use, and hidden effects or unstable return identities can surprise consumers.", "useOnlineStatus owns one subscription pattern and cleans it up for each consuming component.", "Calling the same custom hook in two components automatically shares one state instance.", "Custom hooks share logic, not state; each call has its own hook state unless both connect to shared external data.", "share logic, not state"],
  "react.routing.routes": ["Client routing maps URLs to views while preserving addressability, history, refresh behavior, and server fallback configuration.", "Nested routes, base paths, loaders, not-found states, and direct deployment requests require router-specific setup.", "Navigating to /courses/c renders the course view and refreshing that URL is served by configured hosting.", "A client router makes every deep URL work on the server automatically.", "Configure hosting for direct route requests and test navigation, refresh, back, forward, and not-found behavior.", "direct route requests"],
  "react.routing.params": ["Route parameters and search values are untrusted URL strings that need decoding, validation, defaults, and canonicalization.", "Missing, repeated, malformed, or stale values can select unavailable resources and effects can race.", "A numeric page parameter is parsed, range-checked, and replaced with a safe default when invalid.", "A typed route declaration guarantees every parameter value is valid domain data.", "Validate route strings at the boundary before using them as identifiers, numbers, or trusted navigation targets.", "Validate route strings"],
  "react.routing.focus": ["Client navigation must update document title and move or announce focus context so keyboard and screen-reader users perceive the new view.", "Focusing every route can disrupt restored history position, dialogs need separate rules, and hash navigation has native expectations.", "After a course route changes, the main heading receives managed focus without trapping subsequent navigation.", "Changing visible React components automatically announces a new page to assistive technology.", "Manage title and focus or announcements on client route changes; visual replacement alone may be silent.", "Manage title and focus"],
  "react.context.services": ["Context can provide stable services such as analytics, API clients, or feature policy to a subtree with an explicit provider boundary.", "Mutable service instances, missing providers, server isolation, and test substitution require deliberate lifecycle design.", "A TutorApi interface is provided once and replaced with a fake provider in component tests.", "Importing a global singleton is always equivalent to a contextual service provider.", "Context makes service dependency and substitution explicit; avoid hidden mutable global singletons.", "dependency and substitution explicit"],
  "react.robustness.semantics": ["React output should use native semantic elements, accessible names, keyboard behavior, and state attributes just like authored HTML.", "Component abstraction can hide invalid nesting, generated ids must remain stable, and portals change DOM placement.", "A reusable dialog composes a real button trigger, labeled dialog region, focus management, and escape behavior.", "Because React handles events, div elements are preferable to native buttons.", "Use native semantic controls in React; event handling does not supply keyboard, role, name, or state automatically.", "native semantic controls"],
  "react.robustness.errors": ["React error boundaries catch render-tree errors below them and show recovery UI, while async, event, and server errors need their own handling.", "Boundaries do not catch their own errors, reset strategy matters, and logging must avoid sensitive data.", "A route boundary reports a safe incident id and lets the learner retry without exposing stack details.", "One error boundary catches rejected fetches and every event-handler exception automatically.", "Error boundaries cover descendant rendering failures; handle async and event errors at their actual boundaries.", "rendering failures"],
  "react.robustness.performance": ["React performance work starts with measured user impact, component render cost, network, bundle, and DOM evidence.", "Development timings differ, rerenders can be cheap, virtualization affects accessibility, and premature caching adds complexity.", "Profiler evidence identifies a slow 5000-row render before pagination or virtualization is selected.", "Every rerender is a performance bug that should be prevented.", "Rerenders are normal; optimize measured expensive work and verify the user-visible improvement.", "measured expensive work"],
  "react.robustness.memo": ["memo, useMemo, and useCallback cache work or identities as performance hints when dependencies and comparison costs justify them.", "Caches can be discarded, stale dependencies cause bugs, new object props defeat memo, and caching can cost more than recomputation.", "A measured expensive filter uses useMemo with complete dependencies after simpler render computation proves slow.", "useMemo is required to make a computed value semantically correct and permanent.", "Memoization is an optimization, not a correctness guarantee; first make logic pure and dependencies complete.", "not a correctness guarantee"],
  "react.testing.queries": ["React tests should query by accessible role, name, label, and visible text in the same priority users perceive.", "Some hidden or presentation nodes lack roles, duplicate names need scoping, and test ids are a last resort.", "A save button is selected by role and accessible name rather than its CSS class.", "data-testid is always the most stable and user-centered query.", "Prefer semantic user-facing queries and use test ids only when no meaningful accessible selector exists.", "semantic user-facing queries"],
  "react.testing.async": ["Async React tests trigger realistic actions and await observable state with find or wait utilities instead of fixed sleeps.", "Fake timers need controlled flushing, unresolved promises leak, and waiting for implementation calls can miss user behavior.", "A test clicks Load and awaits the course heading after a mocked response while also covering an error state.", "Adding a one-second timeout is the most reliable way to wait for React updates.", "Await the observable condition rather than elapsed time, and control promises or timers deterministically.", "observable condition"],
  "react.testing.routes": ["Route tests render with router context and assert URL-driven views, navigation, parameters, focus, and not-found behavior.", "A mocked router can diverge from production, base paths matter, and direct-entry behavior needs integration coverage.", "A test starts at /courses/c, activates Python, and asserts URL, heading, and focus context.", "Testing the component alone proves route configuration and browser history behavior.", "Test components inside representative routing context and cover direct entry plus navigation history.", "routing context"],
  "react.project.spa": ["A React SPA project integrates semantic components, state ownership, data states, routes, errors, tests, build, and deployment fallback.", "A polished success path can hide hydration, direct-route, offline, stale, mobile, accessibility, and privacy failures.", "A portfolio app supports direct route refresh, loading and error recovery, keyboard navigation, tests, and a verified production bundle.", "Finishing all visible screens is sufficient evidence that an SPA is complete.", "Verify the full state and route model, failure recovery, accessibility, security, tests, bundle, and deployed behavior.", "full state and route model"],
} as const satisfies Readonly<Record<string, WebTeachingSeed>>;
