export type DsaTeachingSeed = readonly [model: string, boundary: string, scenario: string, misconception: string, correction: string, checkpoint: string];

export const DSA_TRANCHE_SEEDS = {
  "dsa.entry.language-readiness": ["DSA implementation begins only after the learner can express functions, collections, recursion, tests, and complexity in one chosen language.", "Concept knowledge can transfer, but syntax, libraries, ownership, equality, and error behavior must be retested after a language switch.", "A learner traces an array algorithm correctly but first demonstrates safe indexing and functions in C++ before coding it.", "Knowing DSA concepts removes the need for implementation-language prerequisites.", "Keep conceptual mastery, but verify language-specific implementation readiness before coding assessed DSA solutions.", "implementation readiness"],
  "dsa.analysis.specification": ["An algorithm specification defines inputs, outputs, constraints, required behavior, and allowed complexity independently of one implementation.", "Examples do not replace the contract, and invalid, empty, duplicate, or large inputs need explicit treatment.", "A two-sum specification states whether indexes or values are returned and whether one pair is guaranteed.", "The function name and sample input fully specify an algorithm problem.", "Write an input-output-constraint contract before selecting a data structure or language implementation.", "input-output-constraint contract"],
  "dsa.analysis.correctness": ["Correctness connects a precondition, invariant or inductive claim, termination, and postcondition for every permitted input.", "Passing tests supplies evidence but cannot cover an unbounded input domain or repair an invalid invariant.", "A loop invariant states that the retained maximum equals the maximum of all elements already visited.", "Many passing random tests prove an algorithm correct for all inputs.", "Use tests plus a correctness argument showing invariant preservation, termination, and the required postcondition.", "correctness argument"],
  "dsa.analysis.asymptotic": ["Asymptotic analysis describes growth in operations or space as input size increases using upper, lower, and tight bounds.", "Constants, input distribution, multiple parameters, amortization, and memory locality still matter for real constraints.", "Two nested full n loops yield quadratic work while two consecutive n loops remain linear.", "Big-O gives the exact runtime in seconds for every machine and input.", "Big-O describes growth, not exact time; state the input measure, case, and relevant space separately.", "growth, not exact time"],
  "dsa.arrays.operations": ["Arrays support constant-time indexed access while insertion or deletion away from the end usually shifts a linear suffix.", "Dynamic arrays add capacity and amortized append, while bounds, element size, and language representation differ.", "Inserting at index two of five values shifts three existing values before writing the new one.", "Every array operation is constant time because indexing is constant time.", "Separate indexed access from structural updates; middle insertion and deletion require linear movement in an array.", "linear movement"],
  "dsa.arrays.two-dimensional": ["A two-dimensional array models rows and columns, and traversal order determines coordinates, cache behavior, and execution count.", "Rows can be jagged in some languages, C multidimensional types encode stride, and empty dimensions need explicit handling.", "A row-major traversal visits grid[r][c] for each column before advancing to the next row.", "Two nested loops over r rows and c columns always cost O(r+c).", "Visiting every cell costs O(r*c); preserve row lengths and language-specific stride or jagged rules.", "O(r*c)"],
  "dsa.strings.algorithms": ["String algorithms operate on sequences of symbols, with indexing, equality, normalization, and encoding defined by the problem and language.", "Bytes, code points, grapheme clusters, case folding, and immutable representations are not interchangeable.", "A palindrome check defines whether punctuation and case are normalized before comparing mirrored symbols.", "One character always occupies one byte and one index in every language.", "Declare the text unit and normalization rules before indexing or comparing a string.", "text unit"],
  "dsa.arrays.prefix-difference": ["A prefix array precomputes cumulative values for constant-time range queries, while a difference array records boundary changes for batch range updates.", "Index conventions, inclusive endpoints, overflow, and inverse reconstruction must remain consistent.", "Prefix[r+1]-prefix[l] returns the sum of the half-open range [l,r+1).", "Prefix sums make both arbitrary updates and queries constant time without tradeoffs.", "Prefix sums accelerate static range queries; updates usually require rebuilding unless a different structure is chosen.", "static range queries"],
  "dsa.lists.singly": ["A singly linked list stores nodes with values and next links, supporting head updates without shifting contiguous elements.", "Random access remains linear, ownership and deallocation differ by language, and a lost link can orphan the suffix.", "Traversal advances current=current.next until null while retaining the head reference.", "Linked lists provide constant-time access to any index because nodes have pointers.", "Singly linked lists provide sequential traversal; reaching index k requires following k links.", "sequential traversal"],
  "dsa.lists.insert-delete": ["Linked-list insertion and deletion rewire a bounded number of links once the target node or predecessor is known.", "Finding that position can still be linear, and head, tail, empty, and singleton cases need separate invariants.", "Deleting after prev stores prev.next, links around it, then releases or abandons the removed node safely.", "Deleting an item by value is always O(1) in a linked list.", "Link removal is O(1) after locating the predecessor, but locating a value is generally O(n).", "after locating the predecessor"],
  "dsa.lists.doubly-circular": ["Doubly linked lists add previous links, while circular lists replace null ends with links forming a cycle and require stronger consistency invariants.", "Extra links increase memory and update cases, and ordinary null-terminated traversal will not stop on a circular list.", "A circular traversal stops when current returns to head rather than when current becomes null.", "A circular list eventually reaches null after the tail like a singly linked list.", "Circular traversal uses a sentinel or return-to-start condition, and every update preserves both forward and backward links.", "return-to-start condition"],
  "dsa.lists.pointer-patterns": ["Slow-fast pointers, sentinel nodes, and pointer-to-pointer techniques simplify list traversal, cycle detection, and edge-case updates.", "Each technique has a proof obligation and ownership implications rather than being a magic constant-time shortcut.", "Floyd's cycle detector advances one pointer once and another twice until they meet or the fast path ends.", "Slow-fast pointers find a cycle by comparing node values rather than node identity.", "Cycle detection compares node identity; equal values in different nodes do not prove a cycle.", "node identity"],
  "dsa.stack.adt": ["A stack is a last-in-first-out abstract data type with push, pop, top, and emptiness contracts.", "Pop on empty needs defined failure behavior, and array versus linked implementation changes capacity and allocation details.", "Nested delimiters push opening symbols and require each closing symbol to match the current top.", "A stack permits removing any chosen middle element while preserving the ADT.", "Stack removal occurs only at the top under last-in-first-out order.", "last-in-first-out"],
  "dsa.queue.adt": ["A queue is a first-in-first-out abstract data type with enqueue at the back and dequeue at the front.", "Array queues need circular indexing or compaction, and empty/full representation must not be ambiguous.", "A circular buffer advances head and tail modulo capacity without shifting every queued item.", "An array queue must shift every remaining element after each dequeue.", "Use circular indexing to support constant-time enqueue and dequeue without repeated shifting.", "circular indexing"],
  "dsa.deque.adt": ["A deque supports insertion and removal at both front and back while preserving a sequence order.", "It does not imply random middle access, and circular-buffer wraparound requires careful size invariants.", "A sliding-window algorithm removes expired indexes from the front and dominated indexes from the back.", "A deque is just a queue with a faster middle insertion operation.", "A deque exposes both ends, not arbitrary middle insertion; maintain front, back, and size invariants.", "both ends"],
  "dsa.linear.applications": ["Stacks, queues, and deques are selected from the ordering discipline required by the algorithm, not from surface similarity.", "The same problem can require multiple structures, and language library adaptors expose different method names and failure rules.", "Breadth-first search uses a queue while depth-first iterative traversal uses a stack.", "Stack and queue can be exchanged without changing traversal order.", "Choose LIFO for deferred most-recent work and FIFO for arrival-order work; the choice changes observable traversal.", "LIFO",
  ],
  "dsa.hashing.contract": ["Hash tables require equality and hashing to agree so equal keys produce equal hashes and stable key state while stored.", "Collisions are expected, mutable keys can become unreachable, and iteration order is generally not a sorting guarantee.", "A case-insensitive key hashes the same normalized representation used by equality.", "A good hash function eliminates every collision.", "Collisions remain possible; the essential contract is that equal keys have equal hashes.", "equal keys have equal hashes"],
  "dsa.hashing.collisions": ["Collision resolution uses separate chaining or open addressing to distinguish unequal keys sharing a bucket.", "Probe deletion needs tombstones, clustering affects cost, and adversarial hashes can degrade expected constant time.", "Linear probing continues until the key or a truly empty slot, not merely the first deleted slot, is resolved.", "Two different keys with one hash value overwrite each other automatically.", "A collision strategy stores and compares colliding keys rather than treating the hash as unique identity.", "compares colliding keys"],
  "dsa.hashing.resize": ["Hash tables resize and rehash when load policy threatens probe or chain performance, producing amortized rather than worst-case constant insertion.", "Capacity choice, maximum load, incremental rehash, and allocation failure vary by implementation.", "Doubling capacity requires recomputing each bucket because bucket index depends on the new capacity.", "Existing bucket indexes remain valid after capacity changes.", "Rehash stored entries when capacity changes; bucket placement depends on both hash and current capacity.", "Rehash stored entries"],
  "dsa.hashing.patterns": ["Hash-based patterns trade memory for fast expected lookup in frequency counting, membership, indexing, grouping, and prefix-state matching.", "Ordering requirements, worst-case guarantees, key mutation, and bounded alphabets can favor other structures.", "A frequency map finds the first repeated value while preserving a separate traversal for order.", "A hash map is always better than an array for counting values.", "Choose hashing when key range is broad and expected lookup fits; bounded dense keys may favor an array.", "bounded dense keys"],
  "dsa.search.linear-binary": ["Linear search needs no ordering, while binary search maintains a sorted search interval and discards half after each comparison.", "Binary search needs monotonic structure, careful half-open bounds, and overflow-safe midpoint calculation.", "lower_bound searches [lo,hi) for the first position whose value is not less than target.", "Binary search works on any unsorted array if the midpoint is updated correctly.", "Binary search requires a sorted or monotonic predicate and a maintained interval invariant.", "monotonic predicate"],
  "dsa.sort.elementary": ["Insertion, selection, and bubble sort expose different invariants, stability, adaptiveness, and quadratic movement or comparison behavior.", "Small or nearly sorted inputs can favor simple algorithms, but asymptotic and write costs still differ.", "Insertion sort keeps a sorted prefix and inserts the next value into its correct prefix position.", "All quadratic sorting algorithms perform exactly the same comparisons and preserve equal-item order.", "Compare invariants and properties: insertion can be stable and adaptive, while selection minimizes swaps but is typically unstable.", "insertion can be stable and adaptive"],
  "dsa.sort.merge-quick": ["Merge sort divides and merges with predictable O(n log n) time and extra storage, while quicksort partitions and has input-dependent recursion.", "Pivot policy, stability, stack depth, in-place meaning, and worst-case behavior affect selection.", "A merge step combines two sorted half-ranges while preserving their combined order invariant.", "Quicksort is always O(n log n) regardless of pivot and input.", "Quicksort has quadratic worst cases; pivot strategy and introspective fallback manage that risk.", "quadratic worst cases"],
  "dsa.sort.selection": ["Sorting choice follows data size, stability, memory, key cost, existing order, worst-case requirements, and language library guarantees.", "Handwritten algorithms rarely beat mature libraries, and external or parallel data adds different constraints.", "Stable sorting preserves original order among equal scores when earlier registration order is meaningful.", "The asymptotically fastest named sort is always the correct choice.", "Choose from required guarantees and use the standard library sort unless the problem specifically teaches or requires another algorithm.", "required guarantees"],
  "dsa.recursion.design": ["Recursive design identifies a base case, smaller subproblem, combination rule, and progress measure that reaches termination.", "Overlapping work, deep recursion, mutable shared state, and incorrect base coverage can make a valid-looking recurrence fail.", "Tree height returns zero for empty and one plus the maximum height of child subtrees otherwise.", "Any recursive call with a numerically smaller argument guarantees correctness.", "Prove the subproblem is structurally valid and progresses to a base case, then prove the combination rule.", "combination rule"],
  "dsa.recursion.stack": ["Each recursive call adds a frame with parameters and locals until base cases return and frames unwind.", "Stack limits vary, tail-call optimization is not portable across the four languages, and hidden large depth can fail.", "A depth-n linked-list recursion uses n frames even though each frame performs constant local work.", "Recursive algorithms use no extra space when they allocate no heap objects.", "Count call frames as auxiliary space; depth n recursion generally uses O(n) stack space.", "call frames"],
  "dsa.recursion.recurrence": ["A recurrence expresses work in terms of smaller inputs and nonrecursive cost, then expansion or a theorem derives an asymptotic bound.", "Master-theorem cases do not cover every recurrence, floors and uneven splits may need other arguments.", "T(n)=2T(n/2)+O(n) yields O(n log n) for merge sort under its assumptions.", "Dropping the recursive term and keeping only local work gives the total complexity.", "Include every recursive subproblem and combine level work across the recursion tree.", "recursion tree"],
  "dsa.recursion.divide-conquer": ["Divide-and-conquer separates a problem into independent subproblems, solves them recursively, and combines their results.", "Overlapping subproblems favor dynamic programming, unbalanced division harms depth, and combine cost can dominate.", "Merge sort divides into halves and linearly combines sorted results.", "Any algorithm containing recursion is divide-and-conquer.", "Divide-and-conquer requires a meaningful division into subproblems plus a combination step, not recursion alone.", "combination step"],
  "dsa.trees.structure": ["A tree connects nodes by parent-child edges with one root and no cycles, giving depth, height, subtree, and leaf relationships.", "Empty-tree conventions, n-ary children, stored parents, and representation differ across implementations.", "A tree with n nodes has n-1 edges when connected and acyclic.", "Every connected graph is a tree because all nodes are reachable.", "A tree is connected and acyclic; adding one cycle or removing one connecting edge breaks that definition.", "connected and acyclic"],
  "dsa.trees.traversal": ["Preorder, inorder, postorder, and level order visit tree nodes in different structural sequences suited to different tasks.", "Inorder has special meaning for binary trees, recursion depth can overflow, and iterative versions need explicit stack or queue state.", "Postorder processes both child subtrees before deleting or aggregating a node.", "All tree traversals produce the same order because they visit every node once.", "Traversal order changes when the node action occurs relative to children; choose the order from the task.", "relative to children"],
  "dsa.trees.bst": ["A binary search tree maintains an ordering invariant between each node and its subtrees, enabling search proportional to height.", "Duplicate policy must be declared, unbalanced height can become n, and deletion has zero, one, or two-child cases.", "Deleting a two-child node replaces its key with a valid successor then removes that successor from its original place.", "Every binary tree supports logarithmic search by comparing keys.", "Only a maintained search-order invariant enables ordered search, and complexity is O(height), not automatically logarithmic.", "O(height)"],
  "dsa.trees.balance-trie": ["Balanced search trees control height for logarithmic operations, while tries follow key symbols and trade memory for prefix operations.", "Balancing rotations preserve order but are implementation-specific; trie cost depends on key length and alphabet representation.", "A trie retrieves all words under a prefix node without comparing entire unrelated keys.", "A trie operation is O(1) because it does not depend on the number of stored words.", "Trie work depends on key length; balanced-tree work depends logarithmically on stored keys under its invariants.", "key length"],
  "dsa.heaps.property": ["A binary heap is a complete tree whose parent priority dominates children, usually stored compactly in an array.", "Heap order is partial, not full sorting, and min versus max orientation changes the dominant element.", "In a zero-based heap, children of i are 2i+1 and 2i+2 when within size.", "Every level of a heap is globally sorted from left to right.", "A heap guarantees only parent-child priority and root extremum, not total order among siblings or levels.", "parent-child priority"],
  "dsa.heaps.operations": ["Heap insertion bubbles a new element upward, removal replaces the root and restores order downward, and heap construction can run in linear time bottom-up.", "Comparator consistency, index bounds, and mutable priorities can invalidate the structure.", "After removing the minimum, the last element moves to root and sifts down toward the appropriate child.", "Building a heap by bottom-up sifting is O(n log n).", "Bottom-up heap construction is O(n); repeated insertion is the O(n log n) alternative.", "O(n)"],
  "dsa.heaps.priority-queue": ["A priority queue exposes the highest- or lowest-priority item rather than arrival order and is commonly implemented with a heap.", "Equal-priority stability is not automatic, decrease-key support varies, and stale-entry techniques may be needed.", "Dijkstra pushes an improved distance and ignores a popped stale pair whose distance no longer matches the best table.", "A priority queue always dequeues equal-priority items in insertion order.", "Stability among equal priorities requires an explicit tie-breaker; ordinary heap priority alone does not guarantee it.", "explicit tie-breaker"],
  "dsa.heaps.heapsort": ["Heapsort builds a max heap and repeatedly moves the root to the shrinking sorted suffix in O(n log n) time and O(1) auxiliary array space.", "It is typically unstable and can have poorer locality than quicksort or library hybrids.", "After each root swap, sift-down restores heap order only in the unsorted prefix.", "Heapsort is stable because equal values keep heap insertion order.", "Standard in-place heapsort is not stable; heap swaps can reverse equal elements.", "not stable"],
  "dsa.graphs.model": ["A graph models vertices and directed or undirected edges, optionally with weights, labels, self-loops, or parallel edges.", "Problem semantics decide whether direction, multiplicity, and disconnected vertices matter.", "A course prerequisite graph is directed because A requiring B does not imply B requires A.", "Every relationship graph should be treated as undirected for simpler traversal.", "Choose edge direction and graph features from the relationship semantics before selecting an algorithm.", "relationship semantics"],
  "dsa.graphs.representation": ["Adjacency lists use space proportional to vertices plus edges, while matrices use quadratic space and constant-time edge lookup.", "Parallel edges, weights, ordering, mutation, and dense graphs influence the representation.", "A sparse social graph uses adjacency lists rather than allocating a full V by V matrix.", "Adjacency matrices are always more memory efficient because each edge is one Boolean.", "Lists favor sparse graphs; matrices allocate O(V^2) space regardless of edge count.", "O(V^2)"],
  "dsa.graphs.bfs-dfs": ["BFS explores unweighted distance layers with a queue, while DFS explores depth with recursion or a stack.", "Visited state timing matters, recursion can overflow, and neighbor order changes traversal sequence but not reachability.", "BFS marks a vertex when enqueued so it is not added repeatedly by multiple parents.", "BFS and DFS always find the same shortest unweighted path.", "BFS gives shortest edge-count paths in unweighted graphs; DFS does not generally provide that guarantee.", "shortest edge-count paths"],
  "dsa.graphs.components-cycles": ["Connected components partition undirected reachability, while cycle detection tracks parent or active recursion state according to graph direction.", "Directed and undirected cycle rules differ, and self-loops or parallel edges need declared handling.", "Directed DFS finds a back edge to an active recursion-stack vertex as cycle evidence.", "Any edge to an already visited vertex proves a cycle in every DFS.", "Cycle detection must distinguish the parent or completed vertices; only the direction-appropriate state proves a cycle.", "direction-appropriate state"],
  "dsa.graphs.topological": ["A topological order linearizes a directed acyclic graph so every edge goes from an earlier to a later vertex.", "Orders may be nonunique, disconnected DAGs are allowed, and a cycle makes completion impossible.", "Kahn's algorithm repeatedly removes zero-indegree vertices and detects a cycle if fewer than V are removed.", "Every directed graph has at least one topological ordering.", "Only DAGs have a topological order; incomplete processing or a back edge exposes a cycle.", "Only DAGs"],
  "dsa.graphs.shortest-path": ["Shortest-path algorithm choice depends on edge weights: BFS for unweighted, Dijkstra for nonnegative weights, and Bellman-Ford when negative edges may occur.", "Negative cycles can make distances undefined, overflow needs care, and heuristics change guarantees.", "Dijkstra finalizes the smallest tentative distance only under nonnegative edge weights.", "Dijkstra remains correct with negative edges as long as there is no negative cycle.", "Dijkstra requires nonnegative edge weights; use an algorithm designed for negative edges otherwise.", "nonnegative edge weights"],
  "dsa.graphs.mst": ["A minimum spanning tree connects all vertices of a weighted undirected graph with minimum total edge weight and no cycles.", "Disconnected graphs yield a forest, equal weights can yield many MSTs, and shortest paths are a different objective.", "Kruskal sorts edges and adds one only when disjoint-set representatives differ.", "A minimum spanning tree also gives the shortest path between every pair.", "MST minimizes total tree weight, not each pairwise route; shortest-path and spanning-tree objectives differ.", "total tree weight"],
  "dsa.graphs.all-pairs": ["All-pairs shortest paths compute distances between every ordered pair using repeated single-source methods or dynamic programming such as Floyd-Warshall.", "Complexity, sparse versus dense graphs, negative edges, and path reconstruction govern selection.", "Floyd-Warshall considers each intermediate vertex and updates dist[i][j] through it.", "Running BFS once computes shortest paths between every pair.", "One BFS is single-source; all-pairs requires a source run for each vertex or an all-pairs algorithm.", "each vertex"],
  "dsa.disjoint-set.adt": ["Disjoint-set union maintains a partition through make-set, find representative, and union operations.", "It tracks connectivity, not paths or deletion, and representative identity is an implementation detail.", "Two vertices are connected exactly when find returns the same representative after processed unions.", "The representative returned by find must be the smallest value in the set.", "Representatives need only be consistent within a set; their concrete identity is not part of the ADT contract.", "not part of the ADT contract"],
  "dsa.disjoint-set.rank": ["Union by rank or size attaches the shallower or smaller tree beneath the larger representative to limit height.", "Rank is a height bound rather than set size when used with compression, and ties require a consistent update.", "Equal-rank roots are joined and the chosen new root's rank increases once.", "Union by rank attaches the larger-rank root under the smaller one.", "Attach the lower-rank root under the higher-rank root to preserve a shallow structure.", "lower-rank root"],
  "dsa.disjoint-set.compression": ["Path compression rewrites parent links during find so visited nodes point closer or directly to the representative.", "Compression changes internal parents but not partition membership and combines with union heuristics for inverse-Ackermann amortized cost.", "A recursive find assigns parent[x] to the representative returned for its current parent.", "Path compression changes which elements belong to each set.", "Path compression changes representation only; it preserves partition membership while shortening future finds.", "preserves partition membership"],
  "dsa.disjoint-set.applications": ["Disjoint-set supports incremental undirected connectivity, Kruskal cycle checks, grouping, and equivalence merging.", "It does not support arbitrary deletions or directed reachability without additional algorithms.", "Adding an undirected edge forms a cycle exactly when its endpoints already share a representative.", "Disjoint-set can answer the actual path between two connected vertices.", "Disjoint-set answers whether sets are connected, not which path connects their members.", "whether sets are connected"],
  "dsa.greedy.strategy": ["A greedy algorithm makes a locally optimal safe choice and needs an exchange, cut, or staying-ahead argument for global correctness.", "Greedy-looking choices fail when future interactions violate the required property.", "Interval scheduling selects the compatible interval with earliest finish and proves an exchange with an optimal solution.", "Choosing the largest immediate reward is always globally optimal.", "A greedy rule is valid only with a correctness argument showing each local choice can belong to an optimal solution.", "correctness argument"],
  "dsa.backtracking.search": ["Backtracking explores a decision tree, abandons partial states that cannot lead to a solution, and restores state before trying alternatives.", "Worst-case search remains exponential, pruning must be sound, and shared mutable state must be undone exactly.", "A Sudoku solver places a candidate, recurses, and clears that cell when the branch fails.", "Backtracking becomes polynomial whenever one pruning condition is added.", "Pruning can reduce explored states but does not automatically change exponential worst-case complexity.", "exponential worst-case"],
  "dsa.dp.memoization": ["Memoization caches results of overlapping subproblems in a top-down recurrence keyed by all state that affects the answer.", "Missing key dimensions gives wrong reuse, recursion depth remains, and cycles need special handling.", "A grid-path memo key includes both row and column rather than row alone.", "Memoization caches function calls safely even when omitted parameters affect the result.", "A memo key must include the complete subproblem state that determines its result.", "complete subproblem state"],
  "dsa.dp.tabulation": ["Tabulation orders subproblems bottom-up so dependencies are available before each state is computed.", "Iteration order, base initialization, impossible states, and memory compression depend on the recurrence graph.", "A coin-change table initializes zero amount and fills larger amounts from previously solved smaller amounts.", "Dynamic-programming table order never matters because all cells are eventually filled.", "Fill states only after their dependencies; derive loop order from the recurrence, not table appearance.", "after their dependencies"],
  "dsa.patterns.two-pointer": ["Two-pointer methods coordinate positions under a monotonic invariant to avoid repeated scanning.", "They often require sorted input or a specific partition property and can fail with negative or nonmonotonic effects.", "In a sorted pair-sum search, a small sum advances left and a large sum retreats right.", "Two pointers improve every nested-loop array problem to linear time.", "Use two pointers only when pointer movement safely eliminates candidates under a proved invariant.", "eliminates candidates"],
  "dsa.patterns.sliding-window": ["A sliding window maintains an aggregate for a contiguous range while expanding and shrinking boundaries under a valid condition.", "Negative values can break monotonic sum logic, fixed and variable windows differ, and counts must be updated symmetrically.", "A longest-distinct substring window removes left characters until the new right character has count one.", "Sliding-window sum logic works unchanged when arbitrary negative values are present.", "The shrink rule needs a monotonic condition; negative contributions can invalidate common variable-window reasoning.", "monotonic condition"],
  "dsa.patterns.monotonic": ["A monotonic stack or deque removes candidates that cannot be the next greater, smaller, minimum, or maximum answer.", "Strict versus nonstrict comparison controls duplicates, and stored indexes are often needed for distance and expiry.", "A next-greater stack pops smaller values when a new greater value resolves them.", "A monotonic stack must contain every previously visited element.", "Remove dominated candidates while preserving the monotonic invariant; discarded items cannot answer any future relevant query.", "dominated candidates"],
  "dsa.patterns.interval": ["Interval algorithms first define endpoint semantics, then sort or sweep to merge, schedule, count overlap, or answer coverage.", "Closed versus half-open boundaries, touching intervals, and tie ordering change results.", "Half-open intervals [1,3) and [3,5) do not overlap, while closed intervals would touch at three.", "Intervals that share an endpoint always overlap.", "Declare endpoint semantics before comparison; touching half-open intervals are nonoverlapping.", "endpoint semantics"],
  "dsa.transfer.language-map": ["A DSA concept keeps one abstract contract while implementations map operations to each language's types, libraries, ownership, and equality rules.", "Library names can look equivalent while invalidation, mutability, overflow, recursion, and complexity guarantees differ.", "A dynamic sequence maps to vector in C++, ArrayList in Java, list in Python, and an explicit buffer policy in C.", "Translating syntax line by line preserves every data-structure guarantee across languages.", "Map the abstract operations and guarantees first, then implement them with each language's actual semantics.", "abstract operations and guarantees"],
  "dsa.transfer.switch-diagnostic": ["Switching DSA language retains conceptual evidence but requires diagnostic checks for syntax, standard structures, ownership, testing, and complexity expression.", "Failure in one implementation facet does not erase concept mastery but does block unverified language-specific mastery.", "A learner explains BFS correctly but retests Java Queue usage and equality before continuing Java submissions.", "Concept mastery automatically proves implementation mastery in every supported language.", "Retain concept mastery while separately retesting the new language's implementation facets.", "separately retesting"],
  "dsa.quality.adversarial-tests": ["Adversarial tests target empty, singleton, duplicate, ordered, reverse, extreme, disconnected, cyclic, and scale cases derived from algorithm assumptions.", "Random tests need trustworthy oracles, overflow and recursion limits are language-specific, and hidden tests should not reward hacks.", "A binary-search suite includes empty, absent, first, last, duplicates, and maximum-size sorted input.", "Large random inputs alone cover every important algorithm boundary.", "Derive adversarial cases from invariants and failure modes, then add randomized comparison to a trusted oracle.", "invariants and failure modes"],
  "dsa.quality.capstone": ["A DSA capstone specifies behavior and constraints, justifies structure and algorithm choice, proves key invariants, tests adversarial cases, and measures complexity.", "A working demo can hide scale failure, language-specific undefined behavior, weak evidence, or an unjustified copied solution.", "A route planner documents graph representation, shortest-path assumptions, tests disconnected and large graphs, and reports complexity.", "Passing the visible examples is sufficient evidence for a portfolio DSA project.", "A capstone needs reproducible correctness, adversarial tests, complexity evidence, and an independently explained implementation.", "reproducible correctness"],
} as const satisfies Readonly<Record<string, DsaTeachingSeed>>;
